package Memory;

/**
 * Initializes a new game of memory.
 */
public final class App {
    /**
     * Main function. Creates an instance of the memory game and runs it.
     */
    public static void main(String[] args) {
        Memory m = new Memory();
        m.runGame();
    }
}
